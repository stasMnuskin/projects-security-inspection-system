import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#FFA500', // צבע כתום
    },
    secondary: {
      main: '#FF8C00', // גוון כתום כהה יותר
    },
  },
});

export default theme;